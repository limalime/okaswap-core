{
  "name": "okaswap",
  "version": "1.0.0",
  "description": "Okaswap Smart Contracts",
  "scripts": {
    "clean": "rimraf ./artifacts ./cache ./typechain-types",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.18.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0",
    "dotenv": "^16.4.0",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "ethers": "^5.7.2"
  }
}
